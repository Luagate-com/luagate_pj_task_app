# 本番ビルド時に API のフル URL を指定する (例: https://api.example.com)
# 開発時は未設定でよい。Vite の dev proxy が /api を localhost:3030 に転送する。
VITE_API_BASE_URL=

# 開発時に Vite proxy の転送先を変えたい場合
# VITE_DEV_API_PROXY_TARGET=http://localhost:3030
